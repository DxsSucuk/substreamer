diff --git a/node_modules/expo-router/build/fork/useLinking.native.js b/node_modules/expo-router/build/fork/useLinking.native.js
index c080a58..ef831a7 100644
--- a/node_modules/expo-router/build/fork/useLinking.native.js
+++ b/node_modules/expo-router/build/fork/useLinking.native.js
@@ -98,6 +98,25 @@ function useLinking(ref, { enabled = true, prefixes, filter, config, getInitialU
     const getInitialURLRef = (0, react_1.useRef)(getInitialURL);
     const getStateFromPathRef = (0, react_1.useRef)(getStateFromPath);
     const getActionFromStateRef = (0, react_1.useRef)(getActionFromState);
+    // patch-package: guard onUnhandledLinking so it never runs a setState before the
+    // navigator commits its first mount. On the Android dev-client the initial-URL
+    // promise can resolve before mount ("Can't perform a React state update on a
+    // component that hasn't mounted yet"). Deferring past the current commit fixes it.
+    const isMountedRef = (0, react_1.useRef)(false);
+    (0, react_1.useEffect)(() => {
+        isMountedRef.current = true;
+        return () => {
+            isMountedRef.current = false;
+        };
+    }, []);
+    const onUnhandledLinkingSafe = (path) => {
+        if (isMountedRef.current) {
+            onUnhandledLinking(path);
+        }
+        else {
+            setTimeout(() => onUnhandledLinking(path), 0);
+        }
+    };
     (0, react_1.useEffect)(() => {
         enabledRef.current = enabled;
         prefixesRef.current = prefixes;
@@ -124,13 +143,13 @@ function useLinking(ref, { enabled = true, prefixes, filter, config, getInitialU
                         const state = getStateFromURL(url);
                         if (typeof url === 'string') {
                             // If the link were handled, it gets cleared in NavigationContainer
-                            onUnhandledLinking((0, extractPathFromURL_1.extractExpoPathFromURL)(prefixes, url));
+                            onUnhandledLinkingSafe((0, extractPathFromURL_1.extractExpoPathFromURL)(prefixes, url));
                         }
                         return state;
                     });
                 }
                 else {
-                    onUnhandledLinking((0, extractPathFromURL_1.extractExpoPathFromURL)(prefixes, url));
+                    onUnhandledLinkingSafe((0, extractPathFromURL_1.extractExpoPathFromURL)(prefixes, url));
                 }
             }
             state = getStateFromURL(url);
